{
	"name": "cloudflare-workers-workshop",
	"description": "Cloudflare Workers Workshop",
	"private": true,
	"devDependencies": {
		"typescript": "5.9.x",
		"wrangler": "4.37.x"
	},
	"scripts": {
		"cf-typegen": "wrangler types",
		"dev": "wrangler dev",
		"check": "tsc && wrangler deploy --dry-run",
		"deploy": "wrangler deploy"
	}
}
